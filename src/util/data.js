export function getProjectIcons(projectName) {
    const projectIcons = [
        "yosemite.jpg",
        "yosemite.jpg",
    ];
    return projectIcons;
}

export function getProjects() {
    const projects = [
        {
            title: "iOS Reminder",
            intro: "XCode",
            name: "ios-reminder",
        },
    ];
    return projects;
}
